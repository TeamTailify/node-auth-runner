{
  "name": "@tailify/auth-runner",
  "version": "1.0.1",
  "description": "Provides a mechanism for authenticating and caching a bearer token for use with stateless task runners such as AWS lambda functions, preventing the need to re-authenticate against a secure API with every single request",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/TeamTailify/node-auth-runner.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/TeamTailify/node-auth-runner/issues"
  },
  "homepage": "https://github.com/TeamTailify/node-auth-runner#readme",
  "devDependencies": {
    "@types/node": "^8.0.47",
    "typescript": "^2.5.3"
  },
  "dependencies": {
    "memcached-promisify": "^2.0.0",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  }
}
